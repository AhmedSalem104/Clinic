const express=require('express');const controller=require('./dashboard.controller');const {requireAuth,requireRoles}=require('../../middleware/auth');const {asyncHandler}=require('../../utils/errors');const router=express.Router();router.use(requireAuth,requireRoles('owner','doctor','reception'));router.get('/today',asyncHandler(controller.today));module.exports={router};
