const express=require('express');const controller=require('./service.controller');const {serviceSchema}=require('./service.validation');const {validate}=require('../../middleware/validate');const {requireAuth,requirePermission}=require('../../middleware/auth');const {PERMISSIONS}=require('../../config/permissions');const {asyncHandler}=require('../../utils/errors');const router=express.Router();router.use(requireAuth);router.get('/',asyncHandler(controller.list));router.post('/',requirePermission(PERMISSIONS.MANAGE_CLINIC),validate(serviceSchema),asyncHandler(controller.create));router.patch('/:id',requirePermission(PERMISSIONS.MANAGE_CLINIC),validate(serviceSchema),asyncHandler(controller.update));module.exports={router};
