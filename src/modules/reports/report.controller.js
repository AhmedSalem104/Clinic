const {ok}=require('../../utils/response');const service=require('../medical-records/medical.service');const patientService=require('../patients/patient.service');const {AppError}=require('../../utils/errors');
const patientSummary=async(req,res)=>{const patientId=Number(req.params.patientId);await service.withAccess(patientId,req.user,async()=>{});const patient=await patientService.getById(patientId,req.user);if(!patient)throw new AppError('المريضة غير موجودة.',404,'PATIENT_NOT_FOUND');const [cases,visits,pregnancies,medications,allergies,labs,ultrasounds,progress]=await Promise.all([service.listCases(patientId),service.listVisits({patientId,pageSize:25,offset:0}),service.listPregnancies(patientId),service.listMedications(patientId),service.listAllergies(patientId),service.listLabs(patientId),service.listUltrasounds(patientId),service.listProgress(patientId,'')]);return ok(res,{patient,cases,visits:visits.rows,pregnancies,medications,allergies,labs,ultrasounds,progress})};module.exports={patientSummary};
