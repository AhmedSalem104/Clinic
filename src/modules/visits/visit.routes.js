const express=require('express');const controller=require('./visit.controller');const {visitSchema}=require('../medical-records/medical.validation');const {validate}=require('../../middleware/validate');const {requireAuth,requirePermission}=require('../../middleware/auth');const {PERMISSIONS}=require('../../config/permissions');const {asyncHandler}=require('../../utils/errors');const router=express.Router();router.use(requireAuth,requirePermission(PERMISSIONS.VIEW_MEDICAL));router.get('/',asyncHandler(controller.list));router.get('/:id',asyncHandler(controller.get));router.post('/',requirePermission(PERMISSIONS.WRITE_MEDICAL),validate(visitSchema),asyncHandler(controller.create));router.post('/:id/complete',requirePermission(PERMISSIONS.WRITE_MEDICAL),asyncHandler(controller.complete));module.exports={router};
