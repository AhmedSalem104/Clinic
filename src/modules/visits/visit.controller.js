const {ok,created}=require('../../utils/response');const {getPagination}=require('../../utils/pagination');const service=require('../medical-records/medical.service');const list=async(req,res)=>{const p=getPagination(req.query);await service.withAccess(Number(req.query.patientId),req.user,async()=>{});const r=await service.listVisits({patientId:Number(req.query.patientId),...p});return ok(res,r.rows,{page:p.page,pageSize:p.pageSize,total:r.total,totalPages:Math.ceil(r.total/p.pageSize)})};const get=async(req,res)=>{const row=await service.getVisit(Number(req.params.id));if(!row) return res.status(404).json({success:false,error:{code:'VISIT_NOT_FOUND',message:'الزيارة غير موجودة.'}});await service.withAccess(row.PatientId,req.user,async()=>{});return ok(res,row)};const create=async(req,res)=>created(res,await service.createVisit(req.body,req));const complete=async(req,res)=>ok(res,await service.completeVisit(Number(req.params.id),req));module.exports={list,get,create,complete};
