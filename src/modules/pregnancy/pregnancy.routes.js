const express=require('express');const controller=require('./pregnancy.controller');const {pregnancySchema,outcomeSchema}=require('../medical-records/medical.validation');const {validate}=require('../../middleware/validate');const {requireAuth,requirePermission}=require('../../middleware/auth');const {PERMISSIONS}=require('../../config/permissions');const {asyncHandler}=require('../../utils/errors');const router=express.Router();router.use(requireAuth,requirePermission(PERMISSIONS.VIEW_MEDICAL));router.get('/',asyncHandler(controller.list));router.post('/',requirePermission(PERMISSIONS.WRITE_MEDICAL),validate(pregnancySchema),asyncHandler(controller.create));router.patch('/:id/outcome',requirePermission(PERMISSIONS.WRITE_MEDICAL),validate(outcomeSchema),asyncHandler(controller.outcome));module.exports={router};
