const {ok,created}=require('../../utils/response');const {getPagination}=require('../../utils/pagination');const service=require('./appointment.service');
const list=async(req,res)=>{const p=getPagination(req.query);const r=await service.list({user:req.user,...p,date:req.query.date||null,status:req.query.status,doctorId:req.query.doctorId?Number(req.query.doctorId):null,serviceId:req.query.serviceId?Number(req.query.serviceId):null,search:String(req.query.search||'').trim().toLocaleLowerCase('ar-EG')});return ok(res,r.rows,{page:p.page,pageSize:p.pageSize,total:r.total,totalPages:Math.ceil(r.total/p.pageSize)})};
const slots=async(req,res)=>ok(res,await service.availableSlots({doctorId:Number(req.query.doctorId),serviceId:Number(req.query.serviceId),date:req.query.date}));
const get=async(req,res)=>{const appointment=await service.getById(Number(req.params.id));if(!appointment)return res.status(404).json({success:false,error:{code:'APPOINTMENT_NOT_FOUND',message:'الموعد غير موجود أو لم يعد متاحًا.'}});if(req.user.role==='doctor'&&appointment.DoctorId!==req.user.doctorId)return res.status(404).json({success:false,error:{code:'APPOINTMENT_NOT_FOUND',message:'الموعد غير موجود أو لم يعد متاحًا.'}});if(req.user.role==='patient'&&appointment.PatientId!==req.user.patientId)return res.status(404).json({success:false,error:{code:'APPOINTMENT_NOT_FOUND',message:'الموعد غير موجود أو لم يعد متاحًا.'}});return ok(res,appointment)};
const create=async(req,res)=>created(res,await service.create(req.body,req));
const reschedule=async(req,res)=>ok(res,await service.reschedule(Number(req.params.id),req.body,req));
const updateStatus=async(req,res)=>ok(res,await service.updateStatus(Number(req.params.id),req.body,req));
module.exports={list,slots,get,create,reschedule,updateStatus};
