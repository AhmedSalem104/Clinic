const {query}=require('../../db/repository');const {sql}=require('../../db/connection');const list=async()=> (await query('SELECT SettingKey,SettingValue,IsSensitive,UpdatedAt FROM Settings ORDER BY SettingKey')).recordset;const upsert=async(key,value,updatedBy)=> (await query(`UPDATE Settings SET SettingValue=@value,UpdatedBy=@updatedBy,UpdatedAt=SYSUTCDATETIME() WHERE SettingKey=@key; IF @@ROWCOUNT=0 INSERT INTO Settings (SettingKey,SettingValue,UpdatedBy) VALUES (@key,@value,@updatedBy); SELECT SettingKey,SettingValue,IsSensitive,UpdatedAt FROM Settings WHERE SettingKey=@key;`,q=>q.input('key',sql.NVarChar(120),key).input('value',sql.NVarChar(sql.MAX),value).input('updatedBy',sql.Int,updatedBy))).recordsets.at(-1)[0];module.exports={list,upsert};
