const express=require('express');const {z}=require('zod');const controller=require('./setting.controller');const {validate}=require('../../middleware/validate');const {requireAuth,requirePermission}=require('../../middleware/auth');const {PERMISSIONS}=require('../../config/permissions');const {asyncHandler}=require('../../utils/errors');const schema=z.object({key:z.string().trim().min(1).max(120),value:z.string().max(10000)});const router=express.Router();router.use(requireAuth,requirePermission(PERMISSIONS.MANAGE_SETTINGS));router.get('/',asyncHandler(controller.list));router.put('/',validate(schema),asyncHandler(controller.upsert));module.exports={router};
