const express=require('express');const controller=require('./pricing.controller');const {pricingSchema}=require('./pricing.validation');const {validate}=require('../../middleware/validate');const {requireAuth,requirePermission}=require('../../middleware/auth');const {PERMISSIONS}=require('../../config/permissions');const {asyncHandler}=require('../../utils/errors');const router=express.Router();router.use(requireAuth);router.get('/',asyncHandler(controller.list));router.post('/',requirePermission(PERMISSIONS.MANAGE_CLINIC),validate(pricingSchema),asyncHandler(controller.create));router.patch('/:id',requirePermission(PERMISSIONS.MANAGE_CLINIC),validate(pricingSchema),asyncHandler(controller.update));router.delete('/:id',requirePermission(PERMISSIONS.MANAGE_CLINIC),asyncHandler(controller.remove));module.exports={router};
