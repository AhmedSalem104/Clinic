const express=require('express');const controller=require('./schedule.controller');const {scheduleSchema,exceptionSchema}=require('./schedule.validation');const {validate}=require('../../middleware/validate');const {requireAuth,requirePermission}=require('../../middleware/auth');const {PERMISSIONS}=require('../../config/permissions');const {asyncHandler}=require('../../utils/errors');const router=express.Router();router.use(requireAuth);router.get('/',asyncHandler(controller.list));router.post('/',requirePermission(PERMISSIONS.MANAGE_CLINIC),validate(scheduleSchema),asyncHandler(controller.create));router.post('/exceptions',requirePermission(PERMISSIONS.MANAGE_CLINIC),validate(exceptionSchema),asyncHandler(controller.addException));module.exports={router};
