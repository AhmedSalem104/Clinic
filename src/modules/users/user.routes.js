const express=require('express');const controller=require('./user.controller');const {userSchema,statusSchema}=require('./user.validation');const {validate}=require('../../middleware/validate');const {requireAuth,requirePermission}=require('../../middleware/auth');const {PERMISSIONS}=require('../../config/permissions');const {asyncHandler}=require('../../utils/errors');const router=express.Router();router.use(requireAuth,requirePermission(PERMISSIONS.MANAGE_USERS));router.get('/',asyncHandler(controller.list));router.post('/',validate(userSchema),asyncHandler(controller.create));router.patch('/:id/status',validate(statusSchema),asyncHandler(controller.status));module.exports={router};
