import { clinicService } from '../services/clinic-service.js';import {escapeHtml,emptyState,statusBadge,icon,toast} from '../core/ui.js';
export async function render(outlet){outlet.innerHTML=`<div class="section-heading"><div><h1>الخدمات</h1><p>مدة أساسية للخدمة تُستخدم كبداية لحساب الانتظار، ثم تُعاير ببيانات الطبيب واليوم.</p></div><button id="toggle-service-form" class="btn btn-primary">${icon('plus')} إضافة خدمة</button></div><section id="service-form-wrap" class="card p-5 mb-5 hidden"><form id="service-form"><div class="form-grid"><div><label class="form-label">اسم الخدمة *</label><input class="input" name="name" required maxlength="160"></div><div><label class="form-label">التصنيف</label><input class="input" name="category" maxlength="100"></div><div><label class="form-label">المدة الأساسية (دقيقة) *</label><input class="input" name="baseDurationMinutes" type="number" min="1" max="480" value="20" required></div><div class="flex items-center gap-5 pt-7"><label class="flex items-center gap-2 text-xs"><input type="checkbox" name="requiresQueue" checked> تحتاج طابور</label><label class="flex items-center gap-2 text-xs"><input type="checkbox" name="requiresBooking" checked> تحتاج حجز</label></div></div><div class="mt-4 flex justify-end"><button class="btn btn-primary">حفظ الخدمة</button></div></form></section><section class="card"><div id="services-table" class="table-wrap"></div></section>`;document.querySelector('#toggle-service-form').addEventListener('click',()=>document.querySelector('#service-form-wrap').classList.toggle('hidden'));document.querySelector('#service-form').addEventListener('submit',async(e)=>{e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget).entries());data.baseDurationMinutes=Number(data.baseDurationMinutes);data.requiresQueue=e.currentTarget.requiresQueue.checked;data.requiresBooking=e.currentTarget.requiresBooking.checked;try{await clinicService.createService(data);toast('تمت إضافة الخدمة');e.currentTarget.reset();load()}catch(err){window.Swal.fire({icon:'error',title:'تعذر الحفظ',text:err.message})}});const load=async()=>{const box=document.querySelector('#services-table');try{const r=await clinicService.services({page:1,pageSize:100});const rows=r.data||[];box.innerHTML=rows.length?`<table class="data-table"><thead><tr><th>الخدمة</th><th>التصنيف</th><th>المدة</th><th>الطابور</th><th>الحجز</th><th>الحالة</th><th>الأطباء</th></tr></thead><tbody>${rows.map(s=>`<tr><td class="font-semibold">${escapeHtml(s.Name)}</td><td>${escapeHtml(s.Category||'—')}</td><td>${s.BaseDurationMinutes} دقيقة</td><td>${s.RequiresQueue?'نعم':'لا'}</td><td>${s.RequiresBooking?'نعم':'لا'}</td><td>${s.IsActive?statusBadge('active'):statusBadge('inactive')}</td><td>${s.DoctorCount||0}</td></tr>`).join('')}</tbody></table>`:emptyState('لا توجد خدمات بعد.')}catch(err){box.innerHTML=`<div class="p-8 text-center text-sm text-red-600">${escapeHtml(err.message)}</div>`}};await load()}
