import { api } from '../core/api-service.js';
import { escapeHtml, formatDateTime, statusBadge, icon, skeleton, emptyState, localDateKey } from '../core/ui.js';

export async function render(outlet) {
  outlet.innerHTML = `<div class="section-heading"><div><h1>لوحة التحكم</h1><p>ملخص تشغيل العيادة اليوم، مع مؤشرات الطابور والحجوزات.</p></div><div class="flex gap-2"><a class="btn btn-primary" data-route="/appointments/new">${icon('plus')} حجز جديد</a><a class="btn btn-secondary" data-route="/patients/new">إضافة مريضة</a></div></div>${skeleton(4)}<div class="mt-6 card p-5">${skeleton(2)}</div>`;
  try {
    const result = await api.get(`/dashboard/today?date=${localDateKey()}`);
    const s = result.summary || {};
    const metrics = [
      ['إجمالي حجوزات اليوم', s.TotalBookings, 'calendar', 'blue'], ['وصلن إلى العيادة', s.ArrivedPatients, 'users', 'green'], ['في الانتظار', s.WaitingPatients, 'clock', 'amber'], ['داخل الكشف', s.InConsultation, 'medical', 'cyan'], ['حالات مكتملة', s.Completed, 'shield', 'green'], ['لم تحضر', s.NoShow, 'calendar', 'red'], ['الأطباء الموجودون', s.ActiveDoctors, 'doctor', 'blue'], ['متوسط الانتظار', `${Math.round(Number(s.AverageWait || 0))} د`, 'clock', 'amber']
    ];
    outlet.innerHTML = `<div class="section-heading"><div><h1>لوحة التحكم</h1><p>ملخص تشغيل العيادة اليوم، مع مؤشرات الطابور والحجوزات.</p></div><div class="flex flex-wrap gap-2"><a class="btn btn-primary" data-route="/appointments/new">${icon('plus')} حجز جديد</a><a class="btn btn-secondary" data-route="/patients/new">إضافة مريضة</a><a class="btn btn-secondary" data-route="/queue">فتح الطابور</a><a class="btn btn-secondary" data-route="/patients">بحث مريضة</a></div></div><div class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-4 gap-4">${metrics.map(([label,value,metricIcon,color])=>`<div class="card metric-card"><div class="flex items-center justify-between"><div class="metric-label">${label}</div><div class="metric-icon" style="color:${color==='red'?'#dc2626':color==='green'?'#16a34a':color==='amber'?'#d97706':color==='cyan'?'#0284c7':'#2563eb'};background:${color==='red'?'#fef2f2':color==='green'?'#f0fdf4':color==='amber'?'#fffbeb':color==='cyan'?'#ecfeff':'#eff6ff'}">${icon(metricIcon)}</div></div><div class="metric-value">${escapeHtml(value ?? 0)}</div></div>`).join('')}</div><div class="grid grid-cols-1 xl:grid-cols-3 gap-5 mt-6"><section class="card xl:col-span-2"><div class="flex items-center justify-between border-b border-slate-100 px-5 py-4"><div><h2 class="font-semibold text-slate-900">أقرب المواعيد</h2><p class="mt-1 text-xs text-slate-500">المواعيد القادمة مرتبة حسب وقت البداية.</p></div><a class="page-link text-xs" data-route="/appointments">عرض الكل</a></div><div class="table-wrap">${result.upcoming?.length ? `<table class="data-table"><thead><tr><th>المريضة</th><th>الطبيب</th><th>الخدمة</th><th>الوقت</th><th>الحالة</th></tr></thead><tbody>${result.upcoming.map((row)=>`<tr><td class="font-semibold text-slate-800">${escapeHtml(row.PatientName)}</td><td>${escapeHtml(row.DoctorName)}</td><td>${escapeHtml(row.ServiceName)}</td><td>${formatDateTime(row.StartAt)}</td><td>${statusBadge(row.Status)}</td></tr>`).join('')}</tbody></table>` : emptyState('لا توجد مواعيد قادمة اليوم.')}</div></section><section class="card"><div class="border-b border-slate-100 px-5 py-4"><h2 class="font-semibold text-slate-900">تنبيهات التشغيل</h2><p class="mt-1 text-xs text-slate-500">نقاط تحتاج متابعة من فريق العيادة.</p></div><div class="space-y-3 p-5"><div class="alert alert-warning"><span>◐</span><div><strong>التوقفات اليوم</strong><div class="mt-1">${Number(s.DoctorPauses || 0)} توقف مسجل</div></div></div><div class="alert alert-warning"><span>◐</span><div><strong>أكبر تأخير</strong><div class="mt-1">يُحسب من الطابور اللحظي عند فتح الشاشة.</div></div></div><div class="rounded-lg bg-slate-50 p-3 text-xs leading-6 text-slate-600">يستخدم النظام Range للوقت المتوقع ولا يعرض وعدًا بدقيقة ثابتة للمريضة.</div></div></section></div>`;
  } catch (error) {
    outlet.innerHTML = `<div class="section-heading"><div><h1>لوحة التحكم</h1><p>تعذر تحميل ملخص اليوم.</p></div></div><div class="card p-8 text-center text-sm text-slate-500">${escapeHtml(error.message)}</div>`;
  }
}

export function mount(outlet) { outlet.querySelectorAll('[data-route]').forEach((link) => link.addEventListener('click', (event) => { event.preventDefault(); window.clinicApp.navigate(link.dataset.route); })); }
