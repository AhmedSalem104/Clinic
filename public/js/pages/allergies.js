import {medicalService} from '../services/medical-service.js';import {renderMedicalPage,input,formatDate,statusBadge,escapeHtml,emptyState} from './medical-common.js';
export async function render(outlet){return renderMedicalPage(outlet,{title:'الحساسيات والموانع',description:'سجل structured للمادة والتفاعل والشدة والحالة، مع تنبيه واضح في ملف المريضة.',formTitle:'إضافة حساسية',load:id=>medicalService.allergies(id),form:()=>[input('المادة / Substance','substance','text',{required:true}),input('التفاعل','reaction','textarea',{max:500}),input('الشدة','severity','select',{options:[['unknown','غير معروفة'],['mild','خفيفة'],['moderate','متوسطة'],['severe','شديدة']]}),input('الحالة','status','select',{options:[['active','نشطة'],['inactive','غير نشطة'],['entered_in_error','أُدخلت بالخطأ']]}),input('ملاحظات','notes','textarea',{span2,max:1000})],submit:d=>medicalService.createAllergy(d),table:rows=>rows.length?`<div class="table-wrap"><table class="data-table"><thead><tr><th>المادة</th><th>التفاعل</th><th>الشدة</th><th>الحالة</th><th>التاريخ</th></tr></thead><tbody>${rows.map(a=>`<tr><td class="font-semibold">${escapeHtml(a.Substance)}</td><td>${escapeHtml(a.Reaction||'—')}</td><td>${escapeHtml(a.Severity||'—')}</td><td>${statusBadge(a.Status)}</td><td>${formatDate(a.RecordedAt)}</td></tr>`).join('')}</tbody></table></div>`:emptyState('لا توجد حساسيات مسجلة.')})}
