import { clinicService } from '../services/clinic-service.js';
import { escapeHtml, emptyState, statusBadge, icon, toast } from '../core/ui.js';
export async function render(outlet){outlet.innerHTML=`<div class="section-heading"><div><h1>الأطباء</h1><p>إدارة الأطباء، تخصصاتهم، خدماتهم وحالتهم التشغيلية.</p></div><button id="toggle-doctor-form" class="btn btn-primary">${icon('plus')} إضافة طبيب</button></div><section id="doctor-form-wrap" class="card p-5 mb-5 hidden"><form id="doctor-form"><div class="form-grid"><div><label class="form-label">الاسم الكامل *</label><input class="input" name="fullName" required maxlength="160"></div><div><label class="form-label">التخصص</label><input class="input" name="specialty" maxlength="160"></div><div><label class="form-label">الهاتف</label><input class="input" name="phone" maxlength="40"></div><div><label class="form-label">البريد الإلكتروني</label><input class="input" name="email" type="email" maxlength="255"></div><div class="span-2"><label class="form-label">نبذة تشغيلية</label><textarea class="textarea" name="bio" maxlength="1000"></textarea></div></div><div class="mt-4 flex justify-end"><button class="btn btn-primary">حفظ الطبيب</button></div></form></section><section class="card"><div id="doctors-table" class="table-wrap"></div></section>`;document.querySelector('#toggle-doctor-form').addEventListener('click',()=>document.querySelector('#doctor-form-wrap').classList.toggle('hidden'));document.querySelector('#doctor-form').addEventListener('submit',async(e)=>{e.preventDefault();try{await clinicService.createDoctor(Object.fromEntries(new FormData(e.currentTarget).entries()));toast('تمت إضافة الطبيب');e.currentTarget.reset();document.querySelector('#doctor-form-wrap').classList.add('hidden');load()}catch(err){window.Swal.fire({icon:'error',title:'تعذر الحفظ',text:err.message})}});const load=async()=>{const box=document.querySelector('#doctors-table');try{const r=await clinicService.doctors({page:1,pageSize:100});const rows=r.data||[];box.innerHTML=rows.length?`<table class="data-table"><thead><tr><th>الاسم</th><th>التخصص</th><th>الهاتف</th><th>المرضى المسندون</th><th>الخدمات</th><th>الحالة</th></tr></thead><tbody>${rows.map(d=>`<tr><td class="font-semibold">${escapeHtml(d.FullName)}</td><td>${escapeHtml(d.Specialty||'—')}</td><td dir="ltr" class="text-right">${escapeHtml(d.Phone||'—')}</td><td>${d.AssignedPatients||0}</td><td>${d.ServiceCount||0}</td><td>${statusBadge(d.Status)}</td></tr>`).join('')}</tbody></table>`:emptyState('لا يوجد أطباء بعد.')}catch(err){box.innerHTML=`<div class="p-8 text-center text-sm text-red-600">${escapeHtml(err.message)}</div>`}};await load()}
